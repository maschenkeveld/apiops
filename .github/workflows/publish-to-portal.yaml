name: Publish to Developer Portal
# Reusable workflow: for each app in apps_list, reads apis/$APP/konnect.yaml and publishes the API
# to the Konnect Developer Portal when portal: true. All IDs (portal, control plane, service) are
# resolved by name at runtime by scripts/publish-api.sh — no OpenBao, no hardcoded IDs.

on:
  workflow_call:
    inputs:
      apps_list:
        description: 'Space-separated list of apps to publish'
        required: true
        type: string
      konnect_region:
        description: 'Konnect region'
        required: false
        type: string
        default: 'eu'
    secrets:
      KONNECT_TOKEN:
        required: true

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    outputs:
      apps_matrix: ${{ steps.generate-matrix.outputs.apps_matrix }}
    steps:
      - name: Generate apps matrix
        id: generate-matrix
        run: |
          apps_array=$(echo "${{ inputs.apps_list }}" | tr ' ' '\n' | jq -R . | jq -s -c .)
          echo "apps_matrix={\"app\":$apps_array}" >> $GITHUB_OUTPUT

  publish-portal:
    name: To Konnect Developer Portal
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.setup.outputs.apps_matrix) }}
    steps:
      - uses: actions/checkout@v7

      - name: Install kongctl
        run: |
          RELEASE=$(curl -sf -H "Authorization: Bearer ${{ github.token }}" \
            https://api.github.com/repos/Kong/kongctl/releases/latest)
          ASSET_URL=$(echo "$RELEASE" | jq -r '
            .assets[] | select(.name == "kongctl_linux_amd64.zip")
            | .browser_download_url')
          [ -n "$ASSET_URL" ] || { echo "Failed to resolve kongctl release URL"; exit 1; }
          curl -L "$ASSET_URL" -o kongctl.zip
          unzip -q kongctl.zip kongctl
          sudo mv kongctl /usr/local/bin/

      - name: Publish
        env:
          KONNECT_TOKEN: ${{ secrets.KONNECT_TOKEN }}
          KONNECT_REGION: ${{ inputs.konnect_region }}
          APPS_LIST: ${{ matrix.app }}
          PUBLISH_MODE: portal
        run: ./scripts/publish-api.sh

  summary:
    name: Summary
    needs: [publish-portal]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Summary
        run: |
          portal_status="${{ needs.publish-portal.result == 'success' && '✅' || '❌' }}"
          echo "# 🌐 Developer Portal" >> $GITHUB_STEP_SUMMARY
          echo "| Step | Apps | Region | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|------|--------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Developer Portal | ${{ inputs.apps_list }} | ${{ inputs.konnect_region }} | $portal_status |" >> $GITHUB_STEP_SUMMARY
